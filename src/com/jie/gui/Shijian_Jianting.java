package com.jie.gui;

public class Shijian_Jianting {
    public static void main(String[] args) {
        /*
        首先需要了解（匿名内部类）：
            引用。方法名（此处为接口时 ）
            不需要一个重写一个实现类
            只需要这样操作即可：
            引用。方法名（接口（）{
                    实现接口的方法即可
            } ）

            *引用.pack（）  此函数用于自适应大小
            **System.exit
            事件监听（实际上是一个接口 需要一个类去实现或者使用匿名内部类的机制更便捷）：
            关闭windows（窗口）
            Frame frame=new Frame();
            frame.addWindowListener(new WindowAdapter() {
            @Overide
            public void windowClosing(WindowEvent e) {
                System.exit(0);r
            }
        });
         */
    }
}
